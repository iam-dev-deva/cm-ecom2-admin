import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add initial dashboard state here
};

const dashboardSlice = createSlice({
  name: 'dashboard',
  initialState,
  reducers: {
    // Add dashboard reducers here
  },
});

export default dashboardSlice.reducer;
